pipeline {
  agent any
  stages {
    stage('Compile') {
      steps {
        dir('./4-cd/Jenkins/Ejercicio1/calculator') {
          sh '''
              chmod +x gradlew
              ./gradlew compileJava
          '''
        }
      }
    }
  }
}
